"scripts": {
    "dev": "vite",
    "build": "mkdir -p dist && echo 'Redirecting...' > dist/index.html",
    "preview": "vite preview",
    "test": "vitest",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
